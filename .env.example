# Copia este archivo a .env y completa las variables
# IMPORTANTE: esta clave SOLO la usa el servidor (server.js). Nunca debe llevar
# el prefijo VITE_ porque eso la incrustaría en el bundle del navegador.
OPENROUTER_API_KEY=tu_api_key_de_openrouter_aqui
VITE_APP_TITLE=Analiza Permiso